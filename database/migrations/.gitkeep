# Database Migrations

Migration files generated by Drizzle Kit will be placed here.

To generate migrations:
```bash
pnpm --filter @repo/db db:generate
```

To run migrations:
```bash
pnpm --filter @repo/db db:migrate
```

To push schema directly (development only):
```bash
pnpm --filter @repo/db db:push
```
